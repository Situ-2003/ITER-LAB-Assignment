#include<stdio.h>
int main()
{
    int i=0,j=0,des=0,rem,temp,num;
    char hexa[100];
    printf("Enter a binary number ");
    scanf("%d",&num);
    temp=num;
    while(temp>0)
    {
        rem=temp%10;
        des=des+rem*p
    }
}